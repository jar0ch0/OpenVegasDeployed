/**
 * OpenVegas e2e helper parity with Pixel Agents webview helper.
 *
 * This is a scaffolding file for Playwright-based avatar/dealer selectors.
 */
export function avatarStateText(selector: string): string {
  return `${selector}:state`;
}
