--- a/include/physics/rigid_body.h
+++ b/include/physics/rigid_body.h
@@ -1,7 +1,7 @@
 #pragma once
 
 #include <glm/glm.hpp>
-#include <glm/gtc/quaternion.hpp>
+#include <glm/gtx/quaternion.hpp>
 #include <memory>
 #include <vector>
 
@@ -12,17 +12,23 @@
     Dynamic,
     Kinematic,
     Static,
+    Trigger,
 };
 
 struct RigidBodyConfig {
     BodyType type = BodyType::Dynamic;
     float mass = 1.0f;
-    float friction = 0.5f;
-    float restitution = 0.3f;
+    float static_friction = 0.5f;
+    float dynamic_friction = 0.4f;
+    float restitution = 0.2f;
     float linear_damping = 0.01f;
     float angular_damping = 0.05f;
     bool gravity_enabled = true;
+    bool continuous_detection = false;
+    uint16_t collision_group = 0xFFFF;
+    uint16_t collision_mask = 0xFFFF;
     glm::vec3 initial_position = {0, 0, 0};
+    glm::quat initial_rotation = glm::identity<glm::quat>();
 };
 
 class CollisionShape;
@@ -33,6 +39,7 @@
     explicit RigidBody(const RigidBodyConfig& config);
     ~RigidBody();
 
+    RigidBody(RigidBody&&) noexcept;
     RigidBody(const RigidBody&) = delete;
     RigidBody& operator=(const RigidBody&) = delete;
 
@@ -42,27 +49,35 @@
     void apply_torque(const glm::vec3& torque);
     void apply_impulse(const glm::vec3& impulse, const glm::vec3& point);
 
+    void set_linear_velocity(const glm::vec3& velocity);
+    void set_angular_velocity(const glm::vec3& velocity);
     glm::vec3 get_linear_velocity() const;
     glm::vec3 get_angular_velocity() const;
 
     void set_position(const glm::vec3& pos);
+    void set_rotation(const glm::quat& rot);
     glm::vec3 get_position() const;
     glm::quat get_rotation() const;
     glm::mat4 get_transform_matrix() const;
 
     void add_collision_shape(std::unique_ptr<CollisionShape> shape);
+    const std::vector<std::unique_ptr<CollisionShape>>& get_shapes() const;
 
     void set_body_type(BodyType type);
     BodyType get_body_type() const;
 
+    void set_collision_filter(uint16_t group, uint16_t mask);
     bool is_sleeping() const;
     void wake_up();
+    void put_to_sleep();
 
     float get_mass() const;
-    void set_mass(float mass);
+    float get_inverse_mass() const;
+    void set_mass(float mass);  // recomputes inertia tensor
     glm::mat3 get_inertia_tensor() const;
+    glm::mat3 get_inverse_inertia_tensor() const;
 
-    uint32_t get_id() const { return m_id; }
+    [[nodiscard]] uint32_t get_id() const { return m_id; }
 
 private:
     void compute_inertia_tensor();
@@ -74,8 +89,11 @@
     glm::vec3 m_force_accumulator{0};
     glm::vec3 m_torque_accumulator{0};
 
+    glm::mat3 m_inertia_tensor{1.0f};
+    glm::mat3 m_inverse_inertia_tensor{1.0f};
     float m_mass;
     float m_inverse_mass;
+
     BodyType m_type;
     bool m_is_sleeping = false;
     bool m_gravity_enabled;
--- a/src/physics/rigid_body.cpp
+++ b/src/physics/rigid_body.cpp
@@ -1,6 +1,7 @@
 #include "physics/rigid_body.h"
 #include "physics/collision_shape.h"
 #include <glm/gtc/matrix_transform.hpp>
+#include <glm/gtx/quaternion.hpp>
 #include <cassert>
 #include <numeric>
 
@@ -10,12 +11,17 @@
 
 RigidBody::RigidBody(const RigidBodyConfig& config)
     : m_id(s_next_id++)
-    , m_position(config.initial_position)
-    , m_rotation(glm::quat(1, 0, 0, 0))
     , m_mass(config.mass)
-    , m_inverse_mass(config.mass > 0 ? 1.0f / config.mass : 0.0f)
     , m_type(config.type)
-    , m_gravity_enabled(config.gravity_enabled) {
+    , m_gravity_enabled(config.gravity_enabled)
+    , m_position(config.initial_position)
+    , m_rotation(config.initial_rotation) {
+
+    m_inverse_mass = (config.type != BodyType::Static && config.mass > 0.0f)
+        ? 1.0f / config.mass
+        : 0.0f;
+
+    compute_inertia_tensor();
 }
 
 RigidBody::~RigidBody() = default;
@@ -24,26 +30,37 @@
     if (m_type == BodyType::Static) return;
     m_force_accumulator += force;
     wake_up();
+    mark_dirty();
 }
 
 void RigidBody::apply_force_at_point(const glm::vec3& force, const glm::vec3& point) {
     if (m_type == BodyType::Static) return;
     m_force_accumulator += force;
     m_torque_accumulator += glm::cross(point - m_position, force);
     wake_up();
+    mark_dirty();
 }
 
 void RigidBody::apply_torque(const glm::vec3& torque) {
     if (m_type == BodyType::Static) return;
     m_torque_accumulator += torque;
     wake_up();
+    mark_dirty();
 }
 
 void RigidBody::apply_impulse(const glm::vec3& impulse, const glm::vec3& point) {
     if (m_type == BodyType::Static) return;
     m_linear_velocity += impulse * m_inverse_mass;
-    m_angular_velocity += glm::cross(point - m_position, impulse);
+    m_angular_velocity += m_inverse_inertia_tensor * glm::cross(point - m_position, impulse);
     wake_up();
+    mark_dirty();
+}
+
+void RigidBody::set_linear_velocity(const glm::vec3& velocity) {
+    if (m_type == BodyType::Static) return;
+    m_linear_velocity = velocity;
+    wake_up();
+    mark_dirty();
 }
 
 glm::vec3 RigidBody::get_linear_velocity() const {
@@ -58,6 +74,7 @@
     if (m_type != BodyType::Dynamic) return;
     m_position = pos;
     wake_up();
+    mark_dirty();
 }
 
 glm::vec3 RigidBody::get_position() const {
@@ -68,8 +85,14 @@
     return m_rotation;
 }
 
+void RigidBody::set_rotation(const glm::quat& rot) {
+    m_rotation = glm::normalize(rot);
+    wake_up();
+    mark_dirty();
+}
+
 glm::mat4 RigidBody::get_transform_matrix() const {
-    glm::mat4 t = glm::translate(glm::mat4(1.0f), m_position);
+    auto t = glm::translate(glm::mat4(1.0f), m_position);
     t *= glm::mat4_cast(m_rotation);
     return t;
 }
@@ -78,6 +101,11 @@
     m_shapes.push_back(std::move(shape));
     compute_inertia_tensor();
 }
+
+const std::vector<std::unique_ptr<CollisionShape>>& RigidBody::get_shapes() const {
+    return m_shapes;
+}
+
 void RigidBody::set_body_type(BodyType type) {
     m_type = type;
     if (type == BodyType::Static) {
@@ -85,13 +113,22 @@
         m_angular_velocity = glm::vec3(0);
         m_inverse_mass = 0.0f;
     }
+    mark_dirty();
 }
 
 BodyType RigidBody::get_body_type() const {
     return m_type;
 }
 
+void RigidBody::set_collision_filter(uint16_t group, uint16_t mask) {
+    m_collision_group = group;
+    m_collision_mask = mask;
+}
+
 bool RigidBody::is_sleeping() const {
+    if (m_type == BodyType::Static) {
+        return true;
+    }
     return m_is_sleeping;
 }
 
@@ -100,10 +137,20 @@
     m_sleep_timer = 0.0f;
 }
 
+void RigidBody::put_to_sleep() {
+    m_is_sleeping = true;
+    m_linear_velocity = glm::vec3(0);
+    m_angular_velocity = glm::vec3(0);
+}
+
 float RigidBody::get_mass() const {
     return m_mass;
 }
 
+float RigidBody::get_inverse_mass() const {
+    return m_inverse_mass;
+}
+
 void RigidBody::set_mass(float mass) {
     m_mass = mass;
     m_inverse_mass = (mass > 0 && m_type != BodyType::Static) ? 1.0f / mass : 0.0f;
@@ -114,14 +162,25 @@
     return m_inertia_tensor;
 }
 
+glm::mat3 RigidBody::get_inverse_inertia_tensor() const {
+    return m_inverse_inertia_tensor;
+}
+
 void RigidBody::compute_inertia_tensor() {
-    m_inertia_tensor = glm::mat3(1.0f);
+    if (m_type == BodyType::Static || m_mass <= 0.0f) {
+        m_inertia_tensor = glm::mat3(0.0f);
+        m_inverse_inertia_tensor = glm::mat3(0.0f);
+        return;
+    }
 
+    m_inertia_tensor = glm::mat3(0.0f);
     for (const auto& shape : m_shapes) {
-        glm::mat3 shape_inertia = shape->compute_inertia(m_mass);
-        m_inertia_tensor += shape_inertia;
+        m_inertia_tensor += shape->compute_inertia(m_mass);
     }
 
+    m_inverse_inertia_tensor = (glm::determinant(m_inertia_tensor) != 0.0f)
+        ? glm::inverse(m_inertia_tensor)
+        : glm::mat3(0.0f);
 }
 
 void RigidBody::integrate(float dt) {
@@ -132,13 +190,16 @@
     glm::vec3 acceleration = m_force_accumulator * m_inverse_mass;
     if (m_gravity_enabled) {
         acceleration += glm::vec3(0, -9.81f, 0);
     }
 
     m_linear_velocity += acceleration * dt;
-    m_angular_velocity += m_torque_accumulator * dt;
+    m_angular_velocity += m_inverse_inertia_tensor * m_torque_accumulator * dt;
 
     m_position += m_linear_velocity * dt;
-    glm::quat angular_delta(0, m_angular_velocity.x * dt * 0.5f, m_angular_velocity.y * dt * 0.5f, m_angular_velocity.z * dt * 0.5f);
+    glm::quat angular_delta(0,
+        m_angular_velocity.x * dt * 0.5f,
+        m_angular_velocity.y * dt * 0.5f,
+        m_angular_velocity.z * dt * 0.5f);
     m_rotation = glm::normalize(m_rotation + angular_delta * m_rotation);
 
     // Damping
@@ -148,6 +209,7 @@
     // Clear accumulators
     m_force_accumulator = glm::vec3(0);
     m_torque_accumulator = glm::vec3(0);
+    mark_dirty();
 
     // Sleep check
     float energy = glm::length2(m_linear_velocity) + glm::length2(m_angular_velocity);
@@ -157,6 +219,10 @@
     } else {
         m_sleep_timer = 0.0f;
     }
+}
+
+void RigidBody::mark_dirty() {
+    m_dirty_flag = true;
 }
 
 } // namespace physics
--- a/src/physics/collision_solver.cpp
+++ b/src/physics/collision_solver.cpp
@@ -1,5 +1,6 @@
 #include "physics/collision_solver.h"
 #include "physics/rigid_body.h"
+#include "physics/collision_shape.h"
 #include <algorithm>
 #include <cmath>
 
@@ -7,15 +8,22 @@
 
 static constexpr int kMaxIterations = 16;
 static constexpr float kPenetrationSlop = 0.005f;
-static constexpr float kBaumgarte = 0.2f;
+static constexpr float kBaumgarte = 0.15f;
+static constexpr float kWarmStartFactor = 0.85f;
 
 struct ContactConstraint {
     RigidBody* body_a;
     RigidBody* body_b;
     glm::vec3 normal;
-    float penetration;
     glm::vec3 contact_point;
+    glm::vec3 r_a;
+    glm::vec3 r_b;
+    float penetration;
     float accumulated_impulse = 0.0f;
+    float accumulated_friction_impulse = 0.0f;
+    float effective_mass = 0.0f;
+    float friction_mass = 0.0f;
+    float bias = 0.0f;
 };
 
 void CollisionSolver::solve(std::vector<ContactManifold>& manifolds, float dt) {
@@ -29,8 +36,18 @@
             constraint.body_a = manifold.body_a;
             constraint.body_b = manifold.body_b;
             constraint.normal = contact.normal;
-            constraint.penetration = contact.penetration;
             constraint.contact_point = contact.point;
+            constraint.penetration = contact.penetration;
+
+            constraint.r_a = contact.point - manifold.body_a->get_position();
+            constraint.r_b = contact.point - manifold.body_b->get_position();
+
+            float inv_mass_sum = manifold.body_a->get_inverse_mass()
+                               + manifold.body_b->get_inverse_mass();
+            constraint.effective_mass = (inv_mass_sum > 0.0f) ? 1.0f / inv_mass_sum : 0.0f;
+
+            constraint.bias = -(kBaumgarte / dt) *
+                std::max(0.0f, contact.penetration - kPenetrationSlop);
 
             constraints.push_back(constraint);
         }
@@ -40,26 +57,39 @@
     for (int iter = 0; iter < kMaxIterations; iter++) {
         for (auto& c : constraints) {
             glm::vec3 relative_velocity =
-                c.body_b->get_linear_velocity() - c.body_a->get_linear_velocity();
+                (c.body_b->get_linear_velocity() + glm::cross(c.body_b->get_angular_velocity(), c.r_b))
+              - (c.body_a->get_linear_velocity() + glm::cross(c.body_a->get_angular_velocity(), c.r_a));
 
             float velocity_along_normal = glm::dot(relative_velocity, c.normal);
 
-            float inv_mass_sum = c.body_a->get_inverse_mass() + c.body_b->get_inverse_mass();
-            if (inv_mass_sum <= 0.0f) continue;
+            float lambda = -(velocity_along_normal + c.bias) * c.effective_mass;
 
-            float bias = -(kBaumgarte / dt) * std::max(0.0f, c.penetration - kPenetrationSlop);
-            float lambda = -(velocity_along_normal + bias) / inv_mass_sum;
-
-            float old_impulse = c.accumulated_impulse;
+            float old_impulse  = c.accumulated_impulse;
             c.accumulated_impulse = std::max(0.0f, c.accumulated_impulse + lambda);
             lambda = c.accumulated_impulse - old_impulse;
 
             glm::vec3 impulse = c.normal * lambda;
 
-            c.body_a->apply_impulse(-impulse, c.contact_point);
-            c.body_b->apply_impulse(impulse, c.contact_point);
+            c.body_a->set_linear_velocity(
+                c.body_a->get_linear_velocity() - impulse * c.body_a->get_inverse_mass());
+            c.body_b->set_linear_velocity(
+                c.body_b->get_linear_velocity() + impulse * c.body_b->get_inverse_mass());
+
+            // Friction
+            glm::vec3 tangent = relative_velocity - c.normal * velocity_along_normal;
+            float tangent_len = glm::length(tangent);
+            if (tangent_len > 1e-6f) {
+                tangent /= tangent_len;
+                float friction_lambda = -glm::dot(relative_velocity, tangent) * c.effective_mass;
+                float max_friction = 0.4f * c.accumulated_impulse;
+                friction_lambda = glm::clamp(friction_lambda, -max_friction, max_friction);
+
+                glm::vec3 friction_impulse = tangent * friction_lambda;
+                c.body_a->set_linear_velocity(c.body_a->get_linear_velocity() - friction_impulse * c.body_a->get_inverse_mass());
+                c.body_b->set_linear_velocity(c.body_b->get_linear_velocity() + friction_impulse * c.body_b->get_inverse_mass());
+            }
         }
     }
 }
 
 
